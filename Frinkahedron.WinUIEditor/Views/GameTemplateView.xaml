<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Frinkahedron.WinUIEditor.Views.GameTemplateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Frinkahedron.WinUIEditor.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Frinkahedron.WinUIEditor.ViewModels"
    xmlns:gtvm="using:Frinkahedron.WinUIEditor.ViewModels.GameTemplateViewModels"
    x:DataType="gtvm:GameTemplateViewModel"
    mc:Ignorable="d"
    >

    <Grid ColumnDefinitions="1*,2*">
        <local:GameObjectTemplateView
            x:Name="templateView"
            Grid.Column="0"
            DataContext="{Binding ActiveGameObject, Mode=TwoWay}"
            />

        <local:VeldridView
            x:Name="veldridPanel"
            Grid.Column="1"
            IsTabStop="True"
            TabIndex="0"
            DataContext="{Binding Renderer, Mode=TwoWay}"/>
    </Grid>
</UserControl>
