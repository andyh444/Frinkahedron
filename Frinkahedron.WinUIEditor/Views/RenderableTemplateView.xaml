<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Frinkahedron.WinUIEditor.Views.RenderableTemplateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Frinkahedron.WinUIEditor.Views"
    xmlns:vm="using:Frinkahedron.WinUIEditor.ViewModels"
    xmlns:gtvm="using:Frinkahedron.WinUIEditor.ViewModels.GameTemplateViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:DataType="gtvm:RenderableTemplateViewModel"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,Auto,Auto">
        <ComboBox Grid.Row="0" />
        <ListView Grid.Row="1" ItemsSource="{Binding EnabledIndices}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="gtvm:EnabledIndexViewModel">
                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked, Mode=TwoWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <local:TransformTemplateView Grid.Row="2" DataContext="{Binding TransformTemplate}" />
    </Grid>
</UserControl>
